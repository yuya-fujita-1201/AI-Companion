# AI Companion アプリ UI/UX 設計書

## デザインコンセプト

このアプリは、ユーザーがAIと自然な会話を通じて関係を深め、AIが会話を記憶しながら成長していく体験を提供します。デザインは**iOS Human Interface Guidelines (HIG)** に準拠し、片手操作を前提としたモバイルファーストの設計とします。

## 画面一覧

### 1. ホーム画面 (Chat)
**主要コンテンツ:**
- チャット履歴のリスト表示 (最新が上)
- ユーザーメッセージとAI応答の吹き出し
- 音声入力ボタン (大きく、画面下部中央)
- テキスト入力フィールド (画面下部)

**機能:**
- テキストメッセージの送信
- 音声入力ボタンの長押しで録音開始、離すと送信
- スクロールで過去の会話履歴を閲覧
- AIの応答を音声で再生 (オプション)

**レイアウト:**
- 上部: ナビゲーションバー (タイトル「AI Companion」)
- 中央: チャット履歴 (ScrollView/FlatList)
- 下部: 入力エリア (テキストフィールド + 音声ボタン)

### 2. 記憶画面 (Memories)
**主要コンテンツ:**
- AIが覚えている記憶のリスト
- 記憶の種類別フィルター (事実、好み、イベント、会話要約)
- 各記憶の重要度と日時

**機能:**
- 記憶の一覧表示
- 記憶の詳細表示 (タップで展開)
- 記憶の検索機能
- 記憶の削除 (スワイプ操作)

**レイアウト:**
- 上部: セグメントコントロール (全て/事実/好み/イベント)
- 中央: 記憶リスト (カード形式)
- 各カードには記憶内容、日時、重要度を表示

### 3. 設定画面 (Settings)
**主要コンテンツ:**
- 音声入力の有効/無効
- 音声出力の有効/無効
- AIの性格設定 (将来的な拡張)
- 会話履歴のクリア
- アプリ情報

**機能:**
- 各種設定のトグル
- 会話履歴の全削除 (確認ダイアログ付き)

**レイアウト:**
- 標準的なiOS設定画面スタイル
- セクション分けされたリスト

## 主要なユーザーフロー

### フロー1: テキストチャット
1. ユーザーがホーム画面でテキスト入力フィールドをタップ
2. メッセージを入力して送信ボタンをタップ
3. メッセージが吹き出しとして表示される
4. AIが応答を生成中はローディングインジケーター表示
5. AI応答が吹き出しとして表示される
6. 会話が記憶として自動保存される

### フロー2: 音声入力
1. ユーザーがホーム画面で音声入力ボタンを長押し
2. 録音開始 (ボタンがアニメーション)
3. ユーザーが話す
4. ボタンを離すと録音終了、音声認識開始
5. 認識されたテキストが吹き出しとして表示される
6. AIが応答を生成して表示
7. AI応答を音声で再生 (設定で有効な場合)

### フロー3: 記憶の確認
1. ユーザーがタブバーから「Memories」をタップ
2. 記憶一覧が表示される
3. セグメントコントロールで記憶の種類を絞り込み
4. 記憶カードをタップして詳細を確認
5. スワイプで記憶を削除 (確認ダイアログ付き)

## カラーパレット

**ブランドカラー:**
- Primary: `#0a7ea4` (青緑、信頼感と知性を表現)
- Background (Light): `#ffffff` (清潔感)
- Background (Dark): `#151718` (落ち着いた雰囲気)
- Surface (Light): `#f5f5f5` (カード背景)
- Surface (Dark): `#1e2022` (カード背景)

**テキストカラー:**
- Foreground (Light): `#11181C` (主要テキスト)
- Foreground (Dark): `#ECEDEE` (主要テキスト)
- Muted (Light): `#687076` (副次的テキスト)
- Muted (Dark): `#9BA1A6` (副次的テキスト)

**機能カラー:**
- Success: `#22C55E` / `#4ADE80` (成功、記憶保存完了)
- Warning: `#F59E0B` / `#FBBF24` (警告)
- Error: `#EF4444` / `#F87171` (エラー)

## タイポグラフィ

- **大見出し**: 32px, Bold (画面タイトル)
- **見出し**: 20px, Semibold (セクション見出し)
- **本文**: 16px, Regular (チャットメッセージ)
- **キャプション**: 14px, Regular (日時、メタ情報)
- **小さいテキスト**: 12px, Regular (補足情報)

## インタラクション

### 音声入力ボタン
- 通常時: 大きな円形ボタン (64px)、Primary色
- 長押し中: スケールアップ (1.1倍) + パルスアニメーション
- 録音中: 波形アニメーション
- 離した時: スケールダウン + ハプティックフィードバック (Medium)

### メッセージ送信
- 送信ボタンタップ: スケール (0.97) + ハプティックフィードバック (Light)
- メッセージ表示: フェードイン (250ms)
- AI応答生成中: タイピングインジケーター (3つの点が点滅)

### 記憶カード
- タップ: 不透明度 0.7 + 詳細展開アニメーション
- スワイプ削除: スライドアウト + 確認ダイアログ

## アクセシビリティ

- すべてのインタラクティブ要素は最小44x44ptのタップ領域を確保
- 音声入力は視覚的フィードバックと併用 (録音中の視覚的インジケーター)
- カラーコントラスト比はWCAG AA基準を満たす
- ダークモード対応

## 技術的制約

- React Native / Expo SDK 54
- NativeWind (Tailwind CSS) によるスタイリング
- iOS/Android両対応
- オフライン時の挙動: ローカルストレージに保存、オンライン復帰時に同期
